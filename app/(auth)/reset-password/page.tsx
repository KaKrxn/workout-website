import { ResetPasswordForm } from "../password-forms";

export const metadata = { title: "Reset password · FitTrack" };
export const dynamic = "force-dynamic";

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
